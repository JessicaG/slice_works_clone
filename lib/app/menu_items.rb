class MenuItems
  attr_reader :id,
              :name,
              :description,
              :price,
              :category,
              :menu

  def initialize(data)
    @id           = id,
    @name         = name,
    @description  = description,
    @price        = price,
    @category     = category,
    @menu         = menu
  end

end
